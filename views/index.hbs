<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Show Products</title>
  <!-- Thêm các thư viện CSS và Bootstrap nếu cần -->
  <link rel="stylesheet" href="path/to/your/css/style.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>

<body>
  <div class="container mt-5">
    <h1>Show Products</h1>

    <!-- Kiểm tra nếu có sản phẩm Robot để hiển thị -->
    {{#if robots.length}}
      <h2>Robots</h2>
      <div class="row mt-4">
        {{#each robots}}
          <div class="col-md-4 mb-4">
            <div class="card">
              <img src="{{ image }}" class="card-img-top" alt="{{ name }}">
              <div class="card-body">
                <h5 class="card-title">{{ model }}</h5>
                <p class="card-text">Price: {{ price }}</p>
              </div>
            </div>
          </div>
        {{/each}}
      </div>
    {{else}}
      <p>No robot products available.</p>
    {{/if}}

    <!-- Kiểm tra nếu có sản phẩm Doll để hiển thị -->
    {{#if dolls.length}}
      <h2>Dolls</h2>
      <div class="row mt-4">
        {{#each dolls}}
          <div class="col-md-4 mb-4">
            <div class="card">
              <img src="{{ image }}" class="card-img-top" alt="{{ model }}">
              <div class="card-body">
                <h5 class="card-title">{{ model }}</h5>
                <p class="card-text">Price: {{ price }}</p>
              </div>
            </div>
          </div>
        {{/each}}
      </div>
    {{else}}
      <p>No doll products available.</p>
    {{/if}}

    <!-- Thêm các liên kết hoặc nút chức năng khác nếu cần -->
  </div>

  <!-- Thêm các script JS và Bootstrap nếu cần -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-DfXdz2htPH0lsSSs5nCTpuj/zy4C+OGpamoFVy38MVBnE+IbbVYUew+OrCXaRkfj"
    crossorigin="anonymous"></script>
</body>

</html>
